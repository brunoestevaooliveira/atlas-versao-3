# ============================================
# TEMPLATE DE VARIÁVEIS PARA DOCKER
# ============================================
# Este arquivo é usado para configurar variáveis de ambiente no Docker.
# Copie este arquivo para .env.docker.local e preencha com suas credenciais.
#
# IMPORTANTE:
# - .env.docker.local NUNCA será commitado (está no .gitignore)
# - Este arquivo é apenas um template (valores de exemplo)
# - Use o script: .\docker-setup.ps1 para configurar automaticamente
# ============================================

# ============================================
# FIREBASE CONFIGURATION
# ============================================
NEXT_PUBLIC_FIREBASE_PROJECT_ID=seu_project_id
NEXT_PUBLIC_FIREBASE_APP_ID=seu_app_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=seu_storage_bucket
NEXT_PUBLIC_FIREBASE_API_KEY=sua_api_key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=seu_auth_domain
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=seu_messaging_sender_id

# ============================================
# MAPBOX CONFIGURATION
# ============================================
NEXT_PUBLIC_MAPBOX_TOKEN=seu_token_mapbox

# ============================================
# DOCKER SPECIFIC
# ============================================
# Essas variáveis são específicas para o ambiente Docker

# Modo de execução (development ou production)
NODE_ENV=production

# Porta do container (não altere a menos que saiba o que está fazendo)
PORT=3000

# ============================================
# INSTRUÇÕES DE USO
# ============================================
# 1. Copie este arquivo:
#    cp .env.docker .env.docker.local
#
# 2. Edite o .env.docker.local com suas credenciais reais
#
# 3. Ou use o script automatizado:
#    .\docker-setup.ps1
#
# 4. Inicie o Docker:
#    docker-compose up -d prod
# ============================================
